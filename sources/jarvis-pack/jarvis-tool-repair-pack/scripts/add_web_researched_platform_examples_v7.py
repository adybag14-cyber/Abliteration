#!/usr/bin/env python3
"""v7 expansion note.

The v7 data was generated from a curated allow/confirm/block command catalog
based on public Windows, Termux, and macOS command documentation.

This file is intentionally a note rather than a re-generator, because the
expanded examples are already materialized in data/sft_train.jsonl,
data/dpo_train.jsonl, and data/eval_prompts.jsonl.
"""

print("v7 examples are already included in data/. Run scripts/validate_dataset.py")
